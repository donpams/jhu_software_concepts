# Copy to .env (git-ignored) and adjust. Format: postgresql://USER:PASSWORD@HOST:PORT/DBNAME
# Homebrew / Postgres.app on macOS usually trusts the local user, so no password is needed:
DATABASE_URL=postgresql://localhost:5432/gradcafe
# Optional: fixed key for Flask's flash-message cookie
# FLASK_SECRET_KEY=change-me
